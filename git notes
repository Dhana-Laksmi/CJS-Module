git init
git add .
git commit -m "init project"
git remote -v

git push -u -f origin master
git remote set-url origin https://<access token get from developer setting>@github.com/Dhana-Laksmi/<repository-name>.git 
git push origin
git push --set-upstream origin Basic-sorting  //no upstream branch